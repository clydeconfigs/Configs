#########################################
# DEBIAN/HP
# (now known as Arch)
#

exec fish $HOME/Media/Random/archgreponline/archgreponline
exec xautolock -time 10 -locker "i3lock -c 242626"
exec xwallpaper --screen 0 --zoom ~/Media/Random/Pics/cheng-feng-4pTY4ty7jXM-unsplash.jpg
exec setxkbmap es
exec picom
exec dunst
exec xrdb -merge ~/.Xresources
exec $HOME/Media/Code/shell/loop

hide_edge_borders both
for_window [class="^.*"] border pixel 7
for_window [title="^Tor Browser$"] floating enable

default_floating_border pixel 0

font pango:Roboto Mono 16

bindsym $mod+a exec $HOME/Media/Code/rofi/windowcd
bindsym $mod+b exec $HOME/Media/Code/rofi/autobluetooth
bindsym $mod+c exec $HOME/Media/Code/rofi/calculator
bindsym $mod+d exec $HOME/Media/Code/rofi/dmenu
bindsym $mod+e exec $HOME/Media/Code/rofi/emoji
#       $mod+f (USED)
bindsym $mod+g exec $HOME/Media/Code/rofi/google
#       $mod+h (USED)
bindsym $mod+i exec $HOME/Media/Code/rofi/pass 
#       $mod+j (USED)
#       $mod+k (USED)
#       $mod+l (USED)
bindsym $mod+m exec $HOME/Media/Code/rofi/bm3
#       $mod+n (USED)
bindsym $mod+o exec $HOME/Media/Code/rofi/dictionary2
bindsym $mod+p exec $HOME/Media/Code/rofi/genrandom
#       $mod+q (USED)
#       $mod+r (USED)
bindsym $mod+s exec $HOME/Media/Code/shell/sa
#       $mod+t 
bindsym $mod+u exec clav
#       $mod+v (USED)
bindsym $mod+w exec $HOME/Media/Code/rofi/weblinks
#       $mod+x (USED)
#       $mod+y
#       $mod+z (USED)

#       $mod+Shift+a
#       $mod+Shift+b
#       $mod+Shift+c
bindsym $mod+Shift+d exec $HOME/Media/Code/rofi/diccionario
bindsym $mod+Shift+e exec $HOME/Media/Code/rofi/exiti3
#       $mod+Shift+f
bindsym $mod+Shift+g exec $HOME/Media/Code/rofi/translate
#       $mod+Shift+h
#       $mod+Shift+i
#       $mod+Shift+j
#       $mod+Shift+k 
#       $mod+Shift+l
#       $mod+Shift+m
#       $mod+Shift+n
bindsym $mod+Shift+o exec $HOME/Media/Code/rofi/dictionary
#       $mod+Shift+p 
#       $mod+Shift+q
#       $mod+Shift+r (USED)
bindsym $mod+Shift+s exec $HOME/Media/Code/rofi/time
#       $mod+Shift+t
#       $mod+Shift+u
#       $mod+Shift+v
bindsym $mod+Shift+w exec torbrowser-launcher
bindsym $mod+Shift+x exec $HOME/Media/Code/rofi/filemanager
#       $mod+Shift+y
#       $mod+Shift+z (USED)
bindsym $mod+Shift+Escape exec $HOME/Media/Code/rofi/ip

bindsym $mod+Escape workspace next
bindsym $mod+Tab workspace back_and_forth

bindsym $mod+z exec flameshot gui --raw | xclip -sel c

bindsym $mod+x exec EDITOR=micro st -e ranger $HOME/Media/Random/

bindsym $mod+Shift+m --whole-window --border exec i3-volume mute
bindsym $mod+plus exec i3-volume up 10 -P
bindsym $mod+minus exec i3-volume down 10 -P
# bindsym $mod+button4 --whole-window --border exec i3-volume up 5 -P
# bindsym $mod+button5 --whole-window --border exec i3-volume down 5 -P

bindsym $mod+Shift+n gaps inner all set 0px; gaps outer all set 0px
bindsym $mod+n gaps inner all set 20px; gaps outer all set 40px
bindsym $mod+space floating toggle
bindsym $mod+Shift+space focus mode_toggle

bindsym $mod+Return exec st
bindsym $mod+q kill

mode "resize" {
        bindsym h resize grow width 5 px or 10 ppt
        bindsym k resize grow height 5 px or 10 ppt
        bindsym j resize shrink height 5 px or 10 ppt
        bindsym l resize shrink width 5 px or 10 ppt

        bindsym Left resize shrink width 5 px or 1 ppt
        bindsym Down resize grow height 5 px or 1 ppt
        bindsym Up resize shrink height 5 px or 1 ppt
        bindsym Right resize grow width 5 px or 1 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bar {
	mode hide
	modifier Mod1
	strip_workspace_numbers yes
    status_command i3status
    status_command exec $HOME/Media/Code/i3bar/lol.sh
}

set $mod Mod1

exec --no-startup-id dex --autostart --environment i3

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

exec --no-startup-id nm-applet

set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

floating_modifier $mod

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+Shift+v split h
bindsym $mod+v split v

bindsym $mod+f fullscreen toggle

set $ws1 "1󰈹 1"
set $ws2 "2 2"
set $ws3 "3 3"
set $ws4 "4 4"
set $ws5 "5 5"
set $ws6 "6 6"
set $ws7 "7 7"
set $ws8 "8 8"
set $ws9 "9 9"
set $ws10 "10 10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+r mode "resize"

bindsym $mod+Shift+r restart
# bindsym $mod+Shift+c reload
